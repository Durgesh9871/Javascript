// QUESTION - why doctype is used in html ;  

// ANS - Doctype is used to inform the browser about the version of html used in the document. 
// 2. It's called document type deceleration (DTD).  
// 3. This deceleration is very simple in Html file.  

// -----------------------------------------------------------------------------------------------------------


// QUESTION - what is diffrence between HTML and span HTML5 ; --- MAIN QUESTION ;

// ANS - HTML is a primary language for developing web pages; 

//  2. HTML5 is a new version of HTML with some new functionalities, for ex:- HTML does not support audio and video but HTML5 supports both audio and video ;  

// 3.HTML5 -- HTML5 is the 5th version of html with the help of these we can create better and dyanamic website 

// -----------------------------------------------------------------------------------------------------------

// QUESTION - WHAT IS DIFFRENCE BETWEEN DIV AND SPAN TAG ? 

// ANS - Major diffrence is :- 
//  Div - Div is a block level element ;        |      Span - Span is a in-line element ;
//                                              |
//  Div :- Div is like a container and its      |      Span is mostly used to wrap A word and highlighting it;
//  mostly used to wrap a sections              |
//                                              |
                                             

//  -----------------------------------------------------------------------------------------------------------

// QUESTION - What is iframe tag in HTML5 ? 
// ANS -  iframe also know as inline frame ; 

// 2. It's an element that loads another html element inside a web page ; 

// 3. They are commonly used in embeding the external ads , videos 
 

//  -----------------------------------------------------------------------------------------------------------

// QUESTION - What are formatting tags ? 
// ANS - formatting tags are used to change the apprence of the text ; 
// 2. They are used to make the text bold, Italic , Underline etc ;  

// -----------------------------------------------------------------------------------------------------------

// QUESTION - what is diffrence in b and strong tag in html ? 

// ANS :- Bold - Its used to bold the text, So user can notice it , except it , Its does not conatin any importance  ; 

// Strong :- Strong tag shows the strong important of context, Its used to higlight importance things , Its contains very much importance ; 
// 2. We can show the seriousness and importance ; 


// -----------------------------------------------------------------------------------------------------------

// QUESTION - What is view port attribute in html ; 

// Ans - view port is the user's  visible  area of a web page ; Its differs screen to screen ; 

// -----------------------------------------------------------------------------------------------------------

// QUESTION - What is block level element and inline-element ; 
// ANS - Block level element are those element which always start from a new line ; 
// 2. Block level element always take the full width  
// 3. For example - p , div tag and  H1 tag , etc 

// Inline-Elements :- Inline elements are always starts from the same line ; 
// 2. They take only that much space which is needed 
// 3. For example :- span , button etc 


// -----------------------------------------------------------------------------------------------------------
